// console.log(Math.round(1.6)); → 반올림을 의미
console.log(Math.round(1.6)); //반올림으로 2
console.log(Math.round(1.4)); //반올림으로 1


function sum(first,second){ // parameter (매개변수)
  return first+second; // 다음으로 나오는 함수를 종료시키는 역할, 값 도출 역할
}
console.log(sum(2,4)); // argument (인자)
